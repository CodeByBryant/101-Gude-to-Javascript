// Tip #19
// Title: Array.find()
//
// Explanation:
// Returns the first element that satisfies a provided test function.
//
// Example:
const arr = [{ id: 1 }, { id: 2 }];
const result = arr.find(o => o.id === 2);
console.log(result); // { id: 2 }