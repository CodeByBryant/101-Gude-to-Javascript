// Tip #12
// Title: Ternary Operator
//
// Explanation:
// A shorthand way to write conditional expressions in one line: condition ? valueIfTrue : valueIfFalse.
//
// Example:
const age = 20;
const message = age >= 18 ? "Adult" : "Minor";
console.log(message); // Adult